Project : React Counter App
This is a simple React project that demonstrates the use of basic React concepts like useState, event handling, and conditional rendering.

✅ Features Implemented:
A counter that can be increased, decreased, or reset.

The counter value is stored using React's useState hook.

A congratulatory message is displayed when the counter reaches 10, and disappears when it reaches 20.

The reset, decrease button is disabled when the count reaches 0.

The increase button is disabled when count reaches 20 and a message is shown on scrren to inform the user that the count has reached 20.

🔧 Technologies Used:
React (Functional Components + Hooks)

CSS for styling